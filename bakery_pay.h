/***
 * get_pay(int)
 * Returns the per unit pay for an employee
 * 
 * Input:
 * int code: the employee code (1: manager, 2: hourly, 3: decorator, 4: janitor)
 * 
 * Returns: The per unit pay for the given employee
 * 
 */
int get_pay(int code);